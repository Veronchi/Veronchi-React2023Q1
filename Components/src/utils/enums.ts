export enum Paths {
  DEFAULT = '/',
  HOME = 'home',
  ABOUT = 'about-us',
  FORM = 'form',
  ERROR = '*',
}

export enum HeaderClasses {
  LINK = 'header__link',
  PENDING = 'header__link pending',
  ACTIVE = 'header__link active',
}
