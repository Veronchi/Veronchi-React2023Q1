export enum Paths {
  DEFAULT = '/',
  HOME = 'home',
  ABOUT = 'about-us',
  FORM = 'form',
  ERROR = '*',
}

export enum ErrorTypes {
  CHOOSE_SELECT = 'choose option',
}

export enum ErrorMessage {
  SELECT = 'Choose category',
}
